# Core (Colab/Kaggle already ship torch + CUDA; pin only what breaks)
numpy>=1.26
pillow>=10.0
opencv-python-headless>=4.9
scikit-image>=0.22
rasterio>=1.3
tifffile>=2024.1.30
requests>=2.31

# Models
basicsr>=1.4.2          # RRDBNet arch (Real-ESRGAN). Needs the torchvision patch below.
dominate>=2.9           # pytorch-CycleGAN-and-pix2pix dependency

# Data acquisition
earthengine-api>=0.1.400

# Demo + eval
gradio>=4.0
pytorch-fid>=0.3.0

# NOTE (Colab gotcha): basicsr imports torchvision.transforms.functional_tensor,
# removed in torchvision>=0.17. The setup cell in notebooks/colab_driver.ipynb
# applies a one-line sed patch after install — keep it.
